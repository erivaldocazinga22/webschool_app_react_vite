import ProfileModal from "./ProfileModal";
import ProfilePopupRoot from "./ProfilePopupRoot";

const ProfilePopup = {
    Root: ProfilePopupRoot,
    Popup: ProfileModal
}

export default ProfilePopup;